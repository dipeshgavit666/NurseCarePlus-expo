# NurseCare+ (Frontend)

## Setup

1. `npm install`
2. Create `.env` with `EXPO_PUBLIC_API_URL=https://your-backend-url/api`
3. Add `assets/images/icon.png` and `assets/images/android-icon-foreground.png`
4. `npx expo start`
